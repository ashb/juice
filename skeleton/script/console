#!/usr/bin/env flusspferd
// -*- mode:javascript; -*- vim:filetype=javascript:enc=utf-8:
// flusspferd: --interactive

var get, post

// Hide the details
(function() {
  var MyApp = require('../lib/app').juice,
      test = require('juice/test').test(MyApp);

  get = test.get;
  post = test.post;
})()

